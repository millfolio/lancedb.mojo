name = "lancedb-mojo"
version = "0.1.5"
description = "A Mojo LanceDB binding (vector add/search) via FFI to a Rust cdylib; first install compiles the shim"
tags = ["vectors", "database", "lancedb", "ffi"]
# other tins this one depends on; omit if none
tins = []
